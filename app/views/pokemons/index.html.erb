<div class="container">
  <div data-searchbar-target="list" class="cards mt-4">
    <% @pokemons.each do |pokemon| %>
      <%= render 'pokemons/partials/pokemon', pokemon: pokemon %>
    <% end %>
  </div>
</div>
